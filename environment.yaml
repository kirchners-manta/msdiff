# conda env create -f environment.yaml
name: msdiff
channels:
  - defaults
  - conda-forge
dependencies:
  - black
  - coverage
  - lmfit
  - matplotlib
  - numpy
  - pandas
  - pre-commit
  - pylint
  - pytest
  - tox
  - pip:
      - covdefaults
